# databases

Authenticated database inventory route. The page resolves the optional
environment slug before fetching resources or posture totals and fails closed
to an empty result for an unknown scope.
