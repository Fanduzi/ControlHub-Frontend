# tests/app

Vitest tests for the Next.js app boundary.

| File | Coverage |
|------|----------|
| `proxy.test.ts` | Console route guard: valid unexpired Operator Sessions pass; forged, tampered, unknown-key, expired, and unverifiable cookies redirect to login and are cleared (Phase 38X-1C) |

Route-handler tests live under `tests/app/api/` (see its README).

