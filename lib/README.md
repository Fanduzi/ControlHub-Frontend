# lib

Shared frontend libraries.

`auth-role.ts` recovers the presentation-only admin gate from `controlhub.role` storage/cookies (backend 38X-1A+ tokens no longer embed role). Operator Session BFF primitives live in `lib/operator-session/`.
