# components/resources

Resource interaction components.

Admin-only mutation affordances (`CreateResourceSheet`, resource edit, and
archive/restore controls) are presentation hints; the backend remains the
authorization boundary for every write.
